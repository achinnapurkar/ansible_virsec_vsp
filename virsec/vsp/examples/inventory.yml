all: # host group
  vars:
    # Ansible configuration can go here
    ansible_user: linux
    
    #Virsec VSP installation need few more variables, edit those variabled inside respective files (Linux/Windows) under vars directory.

  hosts:
      ansible_host: 127.0.100.1
      ansible_host: 127.0.100.2
